# Page access tokens, one per business. These are secrets — keep this file
# out of git (.gitignore already covers .env).
#
# Generate a long-lived Page token at developers.facebook.com/tools/explorer
# then exchange it for a long-lived one; see README.md.

ROB_PAGE_TOKEN=
RAISING_THINKERS_PAGE_TOKEN=
